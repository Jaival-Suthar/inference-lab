# inference-lab

`inference-lab` is a small open-source foundation for local LLM inference experiments.

## Getting Started

- Install dependencies with `pnpm install`
- Check formatting with `pnpm format:check`
- Lint with `pnpm lint`
- Type-check with `pnpm typecheck`
