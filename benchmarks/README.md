# benchmarks

This folder is reserved for benchmark artifacts, comparison notes, and example outputs.
It is separate from `packages/benchmark`, which contains the benchmark runner implementation.

## Intended Contents

- saved comparison notes
- sample output captures
- benchmark methodology references
- exported results used for documentation

## Why It Exists

Keeping artifacts outside the implementation packages makes it easier to review benchmark history without confusing it
with executable code.
